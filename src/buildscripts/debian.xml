<project name="bmdebian">
    <target name="debclean" extensionOf="clean">
        <exec executable="debuild">
            <arg line="clean"/>
        </exec>
    </target>

    <target name="debuild" extensionOf="artifacts">
        <exec executable="debuild">
            <arg line="binary"/>
        </exec>
    </target>
</project>
